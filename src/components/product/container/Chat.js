import React from 'react';

import ChatPage from '../components/ChatPage';

const Chat = ({ classes, theme }) => {
	
	return (
		<ChatPage
			classes={classes}
			theme={theme}
		/>
	)
}

export default Chat;